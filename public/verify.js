const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const form=document.querySelector('#verify-form'),result=document.querySelector('#verify-result');form?.addEventListener('submit',async event=>{event.preventDefault();const number=new FormData(form).get('certificate').trim();result.innerHTML='<div class="certificate-placeholder"><p>Memeriksa sertifikat…</p></div>';try{const r=await fetch(`/.netlify/functions/verify-certificate?number=${encodeURIComponent(number)}`),data=await r.json();if(!r.ok)throw new Error(data.error);result.innerHTML=`<article class="verification-card ${data.valid?'valid':'invalid'}"><div class="verification-head"><span>${data.valid?'✓':'!'}</span><div><small>Status sertifikat</small><h2>${data.valid?'Valid':'Dicabut'}</h2></div></div><dl><div><dt>Nomor</dt><dd>${esc(data.number)}</dd></div><div><dt>Nama peserta</dt><dd>${esc(data.name)}</dd></div><div><dt>Program</dt><dd>${esc(data.program)}</dd></div><div><dt>Level</dt><dd>${esc(data.level)}</dd></div><div><dt>Diterbitkan</dt><dd>${esc(new Date(data.issuedAt).toLocaleDateString('id-ID'))}</dd></div></dl></article>`}catch(error){result.innerHTML=`<article class="verification-card invalid"><div class="verification-head"><span>!</span><div><small>Hasil pemeriksaan</small><h2>Tidak ditemukan</h2></div></div><p>${esc(error.message)}</p></article>`}});const preset=new URLSearchParams(location.search).get('number');if(preset){form.certificate.value=preset;form.requestSubmit()}
